package models

import (
	"fmt"
	"github.com/coopernurse/gorp"
	"github.com/revel/revel"
	"regexp"
	"time"
)

type {{title .ModelName}} struct {
{{range .Fields}}	{{with .}}{{formatFieldName .Name}} {{formatDataType .Datatype}} `db:"{{.Db_data}}" json:"{{.Json_data}}"`{{end}}
{{end}}}

func ({{firstLetterLower .ModelName}} *{{title .ModelName}}) Validate(v *revel.Validation) {
	/*
	v.Required({{firstLetterLower .ModelName}}.Name).Message("Name Required")
	*/
}